

# Project Overview

## What this project is

A link shortener application. Users authenticate, create short links, and are redirected via those short links.

## Tech Stack

| Layer | Technology |
|---|---|
| Framework | Next.js 16 (App Router) |
| Language | TypeScript 5 (strict mode) |
| Runtime / React | React 19 |
| Styling | Tailwind CSS v4 |
| UI Components | shadcn/ui (`base-nova` style) + Base UI (`@base-ui/react`) |
| Icons | lucide-react |
| Authentication | Clerk v7 (`@clerk/nextjs`) |
| Database | Neon serverless PostgreSQL |
| ORM | Drizzle ORM |
| Animations | tw-animate-css |

## Directory Structure

```
app/             # Next.js App Router pages and layouts
  globals.css    # Tailwind v4 CSS entry + design tokens (@theme)
  layout.tsx     # Root layout — ClerkProvider, fonts
  page.tsx       # Home page

components/
  ui/            # shadcn/ui components (generated — treat as owned code)

db/
  index.ts       # Drizzle client (Neon HTTP driver)
  schema.ts      # Drizzle table definitions

lib/
  utils.ts       # cn() helper (clsx + tailwind-merge)

docs/            # Agent / LLM coding instructions (this directory)

proxy.ts         # Clerk middleware (Next.js middleware file — misnamed; should be middleware.ts if moved)
drizzle.config.ts # Drizzle Kit configuration
```

## Key conventions at a glance

- All imports use the `@/` path alias (maps to project root).
- UI primitives live in `components/ui/` and are generated by shadcn. Do not import from shadcn directly in feature code — always import from `@/components/ui/`.
- All database logic lives in `db/`. Schema changes require a Drizzle migration (`drizzle-kit generate` + `drizzle-kit migrate`).
- Authentication is handled entirely by Clerk — do not build custom auth logic.
- Read the relevant guide in each `docs/` file before writing code in that domain.


# Agent Instructions

Before generating and writing any code, read the relevant file(s) from `docs/`:

| Domain | File |
|---|---|
| Project overview, tech stack, directory structure | [docs/overview.md](docs/overview.md) |
| Next.js 16 App Router, RSC, Server Actions, caching | [docs/nextjs.md](docs/nextjs.md) |
| Drizzle ORM + Neon database, schema, migrations | [docs/database.md](docs/database.md) |
| Clerk v7 authentication, middleware, user context | [docs/auth.md](docs/auth.md) |
| Tailwind CSS v4, shadcn/ui base-nova, cn(), icons | [docs/styling.md](docs/styling.md) |
| TypeScript strict mode, types, conventions | [docs/typescript.md](docs/typescript.md) |

## Non-negotiable rules

1. **Read the docs first.** Do not assume prior knowledge of any library — API surfaces, defaults, and conventions may have changed.
2. **App Router only.** There is no Pages Router. All routes live under `app/`.
3. **Server Components by default.** Only add `"use client"` when strictly necessary.
4. **Never bypass TypeScript.** No `any`, no `// @ts-ignore`.
5. **Database access is server-side only.** Never query the database from a Client Component.
6. **Auth is Clerk.** Do not write custom auth logic.
7. **Styling is Tailwind v4 + shadcn.** No `tailwind.config.js`. Use `cn()` to merge classes.
8. **No raw SQL.** Use the Drizzle query builder. Schema changes require a migration.
9. **Security.** Validate all inputs at Server Action and Route Handler boundaries. Scope all data queries to the authenticated user's ID.
